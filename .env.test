# Configuración de base de datos para tests (usando la misma BD de desarrollo)
DB_HOST=localhost
DB_PORT=5432
DB_TEST_NAME=eventia_db
DB_USER=postgres
DB_PASSWORD=admin123

# Redis Cache (opcional para tests)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
