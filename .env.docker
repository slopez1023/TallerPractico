# Configuración para Docker Compose
# Este archivo muestra las variables que usa docker-compose.yml

# Server
NODE_ENV=production
PORT=3000
API_PREFIX=/api/v1

# Database (PostgreSQL)
DB_HOST=postgres
DB_PORT=5432
DB_NAME=eventia_db
DB_USER=postgres
DB_PASSWORD=admin123

# Cache (Redis)
CACHE_TYPE=redis
REDIS_HOST=redis
REDIS_PORT=6379

# Nota: 
# - Los servicios usan nombres de contenedor (postgres, redis) en lugar de localhost
# - docker-compose.yml ya tiene estas variables definidas
# - Este archivo es solo para referencia
